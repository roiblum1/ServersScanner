# Server Scanner Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# HP OneView
# =============================================================================
ONEVIEW_IP=10.0.0.1
ONEVIEW_USERNAME=administrator
ONEVIEW_PASSWORD=your-password-here

# =============================================================================
# Dell OpenManage Enterprise
# =============================================================================
OME_IP=10.0.0.2
OME_USERNAME=admin
OME_PASSWORD=your-password-here

# =============================================================================
# Cisco UCS Central
# =============================================================================
UCS_CENTRAL_IP=10.0.0.3
UCS_CENTRAL_USERNAME=admin
UCS_CENTRAL_PASSWORD=your-password-here

# UCS Manager credentials (used to connect to individual domains)
UCS_MANAGER_USERNAME=admin
UCS_MANAGER_PASSWORD=your-password-here

# =============================================================================
# Kubernetes Agent Filter (Optional)
# =============================================================================
# Filter out already-installed servers by checking Kubernetes Agent CRD resources
# Uses: oc get agent -A -o wide
K8S_CLUSTER_NAMES=cluster1,cluster2,cluster3
K8S_DOMAIN_NAME=example.com

# RECOMMENDED: Use token authentication (modern Kubernetes)
# Option 1: Single token for all clusters (if they share the same service account)
K8S_TOKEN=your-service-account-token-here

# Option 2: Per-cluster tokens (comma-separated, one per cluster in same order)
# K8S_TOKEN=token-for-cluster1,token-for-cluster2,token-for-cluster3

K8S_NAMESPACE=assisted-installer

# DEPRECATED: Username/password (may not work with modern clusters)
# K8S_USERNAME=admin
# K8S_PASSWORD=your-password-here

# =============================================================================
# Zone Filtering (Optional)
# =============================================================================
# Filter servers by zone names extracted from server profile names
# If not set, all zones will be included
# Examples:
#   ZONES=zone-a,zone-b,zone-c
#   ZONES=prod-zone,staging-zone
# ZONES=
