apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "server-scanner-dashboard.fullname" . }}
  labels:
    {{- include "server-scanner-dashboard.labels" . | nindent 4 }}
data:
  # Cache settings (in-memory TTL for MongoDB reads)
  CACHE_TTL_SECONDS: {{ .Values.config.cacheTtlSeconds | quote }}

  # Server settings
  HOST: {{ .Values.config.host | quote }}
  PORT: {{ .Values.config.port | quote }}
  WORKERS: {{ .Values.config.workers | quote }}

  # Logging
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}

  # Kubernetes settings
  K8S_NAMESPACE: {{ .Values.config.k8sNamespace | quote }}

  # MongoDB
  MONGO_DB_NAME: {{ .Values.config.mongoDbName | quote }}

  # TLS/SSL verification (applies to HP, Dell, Cisco, Kubernetes, MongoDB)
  TLS_VERIFY: {{ .Values.config.tlsVerify | quote }}

  # CronJob sync rate-limit settings
  SYNC_BATCH_SIZE: {{ .Values.config.syncBatchSize | quote }}
  SYNC_BATCH_DELAY: {{ .Values.config.syncBatchDelay | quote }}

  # Feature flags
  ENABLE_CACHING: {{ .Values.config.enableCaching | quote }}
  ENABLE_METRICS: {{ .Values.config.enableMetrics | quote }}
  DEBUG: {{ .Values.config.debug | quote }}
